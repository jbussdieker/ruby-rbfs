#!/usr/bin/env ruby
require 'rbfs'
require 'rbfs/command'

config = Rbfs::Command.config
hosts = Rbfs::Hosts.new(config)
results = hosts.sync
results.each do |host, result|
  puts "#{host}: #{result}"
end
